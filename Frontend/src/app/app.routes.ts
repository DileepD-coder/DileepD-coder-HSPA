import { Routes } from '@angular/router';
import { AppComponent } from './app.component';  // Use an existing component

export const routes: Routes = [
  { path: '', component: AppComponent },  // Route to AppComponent instead
];
